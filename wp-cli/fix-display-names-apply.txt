wp eval '$apply = true; $name = trim(wp_specialchars_decode(get_bloginfo("name"), ENT_QUOTES)); if ($name === "") { WP_CLI::error("Site title is empty, set it first."); } foreach (get_users() as $u) { $d = trim($u->display_name); if ($d === "" || strpos($d, "@") !== false || strcasecmp($d, $u->user_login) === 0) { if ($apply) { $r = wp_update_user(["ID" => $u->ID, "display_name" => $name, "nickname" => $name]); if (is_wp_error($r)) { WP_CLI::warning("User {$u->ID}: " . $r->get_error_message()); continue; } } WP_CLI::log(($apply ? "Fixed" : "[dry run] Would fix") . " user {$u->ID} ({$u->user_login}): \"{$d}\" -> \"{$name}\""); } }'
